Lecture 3

primitive type 基本类型
reference type 引用类型
instantiate 实例化
allocate 分配
Java allocates exactly a box of size 64 bits, no matter what tyoe of object.
引用类型不管什么对象都是64bits，即内存的地址。
对象赋值是把两个对象指向同一块内存。本质是内存地址的复制。

recursive 递归的
subordinate 从属的
sentinel 哨兵
invariant 恒定的

generic 通用的
parametrized 参数化 

Lecture 5
If X is a superclass of Y, then memory boxes for X may contains Y.
Every variable in Java has a compile-time type/static type. This is the type specified at declaration. Never changes.
All constructors must start with a call to one of the super class's constructors.
如果为指定构造函数的super方法，会默认使用无参数的默认构造方法。
encapsulation 封装
Overriding only applies to non-static methods.
Polymorphism: providing a single interface to entities of different types.
